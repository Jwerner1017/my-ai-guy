# AETHER — Sovereign Local Intelligence System
# Production-grade dependencies for maximum capability and reliability

# === Core LLM & Agent Framework ===
langchain==0.2.16
langchain-core==0.2.38
langgraph==0.2.35
langchain-community==0.2.16

# === LLM Providers (local-first) ===
ollama==0.3.3
langchain-ollama>=0.2.0
# openai>=1.40.0          # Optional cloud fallback (user must enable)
# anthropic>=0.34.0       # Optional

# === Memory & Vector Store ===
chromadb==0.5.5
sentence-transformers==3.1.1
networkx==3.3               # For knowledge graph component of memory

# === Voice Stack (Production) ===
faster-whisper==1.0.3
piper-tts==1.2.0
sounddevice==0.4.7          # Audio I/O
webrtcvad==2.0.10           # Voice activity detection
pydub==0.25.1

# === UI ===
gradio==4.44.0
gradio-client==1.3.0

# === Tools & Sandboxing ===
playwright==1.47.0          # Browser automation (best-in-class)
 RestrictedPython==7.0       # Safer code execution sandbox
psutil==6.0.0               # Resource monitoring for sandbox

# === Infrastructure & Config ===
pyyaml==6.0.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
rich==13.8.1                # Beautiful terminal output & logging

# === Async & Performance ===
asyncio==3.4.3
aiohttp==3.10.5
tenacity==8.5.0             # Retry logic with backoff

# === Observability & Tracing ===
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
# opentelemetry-instrumentation-langchain  # Future

# === Security & Permissions ===
cryptography==43.0.1

# === Mobile Companion API & Discovery ===
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
qrcode[pil]==8.0
Pillow==10.4.0
zeroconf==0.132.2

# === Development & Quality (optional but recommended) ===
black==24.8.0
ruff==0.6.8
pytest==8.3.3
pytest-asyncio==0.24.0

# Note: Install playwright browsers separately after pip install:
# playwright install chromium
